<%= form_with(model: all_product) do |form| %>
  <% if all_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(all_product.errors.count, "error") %> prohibited this all_product from being saved:</h2>

      <ul>
        <% all_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :category, style: "display: block" %>
    <%= form.text_field :category %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
