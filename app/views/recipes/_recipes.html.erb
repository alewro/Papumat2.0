  <% @recipes.each do |recipe| %>
  <% if recipe.recipe_category_id == id %>
   
    <div class="d-flex justify-content-between">
      <div class="">
        <%= link_to recipe.name, recipe %>
      </div>
      <div class="">
        <%= link_to "Edit", edit_recipe_path(recipe), class:"btn" %>
      </div>
    </div>
   <% end %>

  <% end %>